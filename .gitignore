# Python
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# uv
# NOTE: uv.lock is intentionally NOT ignored — this is an application, so the
# lockfile belongs in version control for reproducible installs.
.uv-cache/

# Testing & coverage
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/
.nox/

# Type checkers & linters
.mypy_cache/
.pyright/
.ruff_cache/

# Environment / secrets
.env
.env.*
!.env.example

# Editors
.idea/
*.swp
*.swo

# Claude Code local settings (committed docs under .claude/docs/ are kept)
.claude/settings.local.json

# macOS
.DS_Store
